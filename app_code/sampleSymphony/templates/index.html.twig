<html>
    <body>
        <div>
            <p>{{ headerText }}</p>
            <div>
                {% for item in mahList %}
                    <p>{{ item.id }}</p> <p>{{ item.name }}</p> <p>{{ item.price }}</p>
                {% endfor %}
            </div>
            <div>
                {{ myID }}
            </div>
            <div>
                {% for row in customResult %}
                    <p>{{ row.id }}</p> <p>{{ row.name }}</p> <p>{{ row.price }}</p>
                {% endfor %}
            </div>
            <div>
                {% for row in fromCustom %}
                    <p>{{ row.id }}</p> <p>{{ row.name }}</p> <p>{{ row.price }}</p>
                {% endfor %}
            </div>
        </div>
    </body>
</html>